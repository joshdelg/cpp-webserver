### cpp-webserver
A project for me to learn modern C++ and remember how to code without using AI

## Setup Instructions
Initialize the build directory and build the project:
```bash
cmake -B build
cd build
cmake --build . --parallel
```